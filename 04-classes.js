/* 
- static method can call directly without create and instance
- stacic si vuelve a ser declarada en una clase extends, va a sobreescribirla y darle el nuevo valor que deseemos.
- los metodos privados this.#nombre solo se pueden acceder desde la clase y no invocandolo desde un objeto con consol.log(contacto.#nombre)
*/

